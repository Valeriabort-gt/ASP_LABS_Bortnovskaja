@model Application.Organizations.Queres.GetOrganizationList.OrganizationListVm
@{
    ViewData["Title"] = "OrganizationsPage";
}

<style>
    .contractors-container {
        width: 100%;
        position: relative;
        height: 100%;
    }

   .nav-wrapper {
       width: 100%;
       height: 100px;
       display: flex;
       justify-content: flex-end;
       align-items: center;
   }

   .nav-input {
       width: 100px;
       height: 35px;
       border: 1px solid grey;
       color: grey;
       border-radius: 7px;
       padding-left: 30px;
   }

   .nav-button {
       width: 100px;
       height: 39px;
       border-radius: 5px;
       background-color: #F7EF79;
       cursor: pointer;
       transition: 0.3s;
       border: none;
   }

   .contractors-helpbody {
       display: flex;
       width: 100%;
       justify-content: space-between;
       align-items: center;
       margin-top: 20px;
   }

   .contractors-add-button {
       width: 150px;
       height: 39px;
       border-radius: 5px;
       background-color: #F7EF79;
       cursor: pointer;
       transition: 0.3s;
       border: none;
   }

   .contractors-add-button:hover {
       background-color: lightgreen;
       border: 1px solid lightgreen;
   }

   .nav-button:hover {
       background-color: lightgreen;
       border: 1px solid lightgreen;
   }

   button {
        width: 250px;
        height: 50px;
        border-radius: 15%;
        background-color: #F7EF79;
        cursor: pointer;
        transition: 0.3s;
        border: none;
    }
    table, tr, th, td{
        border: 1px solid #8080805c;
        margin-left: 450px;
    }
</style>

<div class="contractors-container">
    <nav class="nav-wrapper">
        <form class="nav-container" action="/organization/Search">
            <input placeholder="Поиск" class="nav-input" name="searchString" type="text" value=@Context.Request.Cookies["lastSearch"] />
            <button class="nav-button" type="submit">Найти</button>   
        </form>
    </nav>
        <table class="table contractors-table">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Name</th>
                    <th scope="col">Email</th>
                    <td scope="col"><b>Изменение</b></td>
                    <td scope="col"><b>Удаление</b></td>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.organizations)
                {
                     <tr>
                        <th scope="row">@item.id</th>
                        <td>@item.name</td>
                        <td>@item.email</td>
                        <td><a href="/organization/detail?id=@item.id">Изменить</a></td>
                        <td><a href="/organization/delete?id=@item.id">Удалить</a></td>
                    </tr>
                }
            </tbody>
        </table>
    

    <div class="contractors-helpbody">
        <nav aria-label="Page navigation example">
        <ul class="pagination">
            <li class="page-item">
                <a class="page-link" href=@Model.back aria-label="Previous">
                    <span aria-hidden="true">&#8592;&nbsp;</span>
                </a>
            </li>
            <li class="page-item">
                <a class="page-link" href=@Model.next aria-label="Next">
                    <span aria-hidden="true">&#8594;</span>
                </a>
            </li>
        </ul>
    </nav>

    <form action="/organization">
        <input type="number" class="nav-input" name="page" max="@Model.pagesCount" min="0"/>
        <button type="submit" class="nav-button">Перейти</button>
    </form>

    <form action="/organization/create">
        <button type="submit" class="contractors-add-button">Добавить</button>
    </form>
    </div>
</div>